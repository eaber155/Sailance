<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.evivian.admin.sailance">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="app.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/boatpicture"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="activity.ClubRegistration"
            android:label="sailance"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name="activity.OpenClubAccount"
            android:label="sailance"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".ClubAccountHome"
            android:label="sailance"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name="clubactivity.NewRaceActivity" />
        <activity android:name="clubactivity.SettingsActivity" />

        <activi android:name="clubactivity.AboutUsActivity" />

        <activity android:name="timer.RecordRaceActivity" />
        <activity android:name="timer.RecordRaceAdapter" />
        <activity android:name="timer.ViewRaceResults" />
        <activity android:name="clubactivity.PersonalHandicapsActivity" />
        <activity android:name="clubactivity.BoatHandicapActivity" />
        <activity android:name="clubactivity.ViewPreviousRaces" />
        <activity android:name="clubactivity.AboutUsActivity"></activity>
    </application>

</manifest>